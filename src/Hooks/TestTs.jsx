// @ts-check

/**
 * @description 두 수의 합을 구하는 함수
 * @param {number} a
 * @param {number} b
 * @returns
 */
function sum(a, b) {
  return a + b;
}

const total = sum(200, 200);

console.log(total.toFixed(2));
